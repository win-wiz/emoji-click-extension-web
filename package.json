{
  "name": "emoji-extension",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "i18next": "^23.7.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-i18next": "^13.5.0",
    "react-scripts": "5.0.1",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "@babel/plugin-transform-runtime": "^7.25.9",
    "@babel/preset-env": "^7.0.0",
    "@babel/preset-react": "^7.0.0",
    "@types/chrome": "^0.0.246",
    "@types/node": "^16.18.0",
    "babel-loader": "^8.0.0",
    "copy-webpack-plugin": "^12.0.2",
    "tailwindcss": "^3.3.0",
    "web-ext": "^8.3.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "webpack --config webpack.config.js",
    "clean": "rm -rf plugins/*",
    "build:chrome": "npm run clean && webpack --config webpack.chrome.config.js",
    "build:firefox": "rm -rf distFirefox && webpack --config webpack.firefox.config.js && web-ext build --source-dir plugins --artifacts-dir distFirefox --overwrite-dest",
    "build:edge": "npm run clean && webpack --config webpack.edge.config.js",
    "build:safari": "npm run clean && webpack --config webpack.safari.config.js",
    "build:all": "npm run clean && npm run build:chrome && npm run build:firefox && npm run build:edge && npm run build:safari",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
